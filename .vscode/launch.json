{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug envtest",
            "type": "go",
            "request": "launch",
            "mode": "test",
            "program": "${fileDirname}",
            "buildFlags": "-tags=envtest",
            "env": {
                "KUBEBUILDER_ASSETS": "${env:HOME}/Library/Application Support/io.kubebuilder.envtest/k8s/1.35.0-darwin-arm64"
            },
            "args": [
                "-test.run",
                "${selectedText}"
            ]
        }
    ]
}
